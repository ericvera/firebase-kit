# Progress

## 1.1 — Bumped the five in-range dependencies and their declared floors

- Key changes:
  - `package.json` (root) — `eslint` `^10.8.0` → `^10.8.1`, `typescript-eslint`
    `^8.66.0` → `^8.67.0`.
  - `packages/firebase-kit-admin/package.json` — `firebase-functions` `^7.2.5` →
    `^7.3.2` in **both** `peerDependencies` and `devDependencies`; `eslint` →
    `^10.8.1`; `scdate-testing` `^7.0.0` → `^7.1.2`.
  - `packages/firebase-kit-client/package.json` — `firebase` `^12.16.0` →
    `^12.17.1` in **both** `peerDependencies` and `devDependencies`; `eslint` →
    `^10.8.1`.
  - `packages/firebase-kit-protocol/package.json` — `eslint` → `^10.8.1`.
  - `packages/firebase-kit-admin/README.md` — `firebase-functions` peer line now
    quotes `^7.3.2`.
  - `packages/firebase-kit-client/README.md` — `firebase` peer line now quotes
    `^12.17.1`.
  - `yarn.lock` — regenerated by `yarn install` (not hand-edited). Resolutions
    now: `eslint` 10.8.1, `typescript-eslint` 8.67.0, `firebase` 12.17.1,
    `firebase-functions` 7.3.2, `scdate-testing` 7.1.2.
- Deviations from plan: none. `firebase-admin`, `firestore-snapshot-utils`, and
  `getsetdel` were deliberately left untouched for tasks 1.2–1.4; `typescript`
  (`^6.0.3`), `@types/node` (`^24.13.3`), and the un-carated `firebase-tools`
  (`15.26.0`) are unchanged.
- Verification: `yarn format` (no changes), `yarn lint`, `yarn build`,
  `yarn test:unit` (48 + 29 files, 329 tests), and `yarn test:emulator`
  (7 files, 21 tests) all passed. The `eslint`/`typescript-eslint` bump surfaced
  no new rule violations. End-to-end tests N/A per the config's
  *consumer-facing wiring* Test exception; its substitute packed-tarball
  consumer check is task 2.1.

## 1.2 — Ported `firestore-snapshot-utils` into `firebase-kit-admin` with new tests

- Key changes:
  - `src/testing/internal/` (new) — `DocumentChangeSnapshot.ts`,
    `ascCompare.ts`, `extractTimestamps.ts`, `maskProps.ts`, `normalizeData.ts`
    copied verbatim from the v4.0.0 local clone (byte-identical; only the
    barrel-level wiring differs).
  - `src/testing/normalizeData.ts` (new) — the public `normalizeData` and
    `NormalizeDataOptions`, `logTimestamps` kept.
  - `src/testing/getDBSnapshot.ts`, `getDBChanges.ts`, `getDBChangesDiff.ts` —
    the three wrappers absorbed the library bodies. `DebugOptions`, the
    `logTimestamps` `console.log` block and the `TimestampDebugOptions`
    construction were dropped as unreachable; `getDBChanges` applies
    `masks ?? {}` in place of the lost default parameter. Public signatures
    unchanged.
  - `src/testing/index.ts` — added `export * from './normalizeData.js'` and
    `export type * from './internal/DocumentChangeSnapshot.js'`, so
    `NormalizeDataOptions`, `DBSnapshotChanges` and the four snapshot class
    types are nameable from `firebase-kit-admin/testing`.
  - New colocated tests: `internal/ascCompare.test.ts`,
    `internal/extractTimestamps.test.ts`, `internal/maskProps.test.ts`,
    `internal/normalizeData.test.ts`,
    `internal/DocumentChangeSnapshot.test.ts`, `normalizeData.test.ts`.
  - Rewrote `getDBSnapshot.test.ts`, `getDBChanges.test.ts` and
    `getDBChangesDiff.test.ts` against the in-tree implementation; no
    `vi.mock('firestore-snapshot-utils', ...)` remains. The old
    `undefined`-vs-`{}` cases were replaced with real masking assertions per
    the task file.
  - `src/__test__/utils/createDocSnapshot.ts` (new) — builds the
    `QueryDocumentSnapshot` slice (`ref.path`, `ref.parent.id`, `updateTime`,
    `data()`) the diffing code reads, so the ported code is unit-testable
    without an emulator.
  - `src/firestore/createFirestoreUtils.emulator.test.ts` and
    `checkDocumentInQueryExists.emulator.test.ts` — `normalizeData` now imports
    from `../testing/normalizeData.js`.
  - `packages/firebase-kit-admin/package.json` — `firestore-snapshot-utils`
    removed from `peerDependencies`, `peerDependenciesMeta` and
    `devDependencies`; `jest-diff` `^30.4.1` added to `dependencies`.
    `firebase-admin` untouched at `^13.10.0`.
  - `packages/firebase-kit-admin/README.md` — optional-peer bullet and install
    snippet entry deleted (prose reworded for the single remaining package);
    the `### firebase-kit-admin/testing` section now documents
    `normalizeData(data, options?)` and the six newly public types.
  - `yarn.lock` — regenerated.
- Deviations from plan: none.
- Verification: `yarn format`, `yarn lint`, `yarn build`, `yarn test:unit`
  (54 + 29 files, 227 + 149 tests) and `yarn test:emulator` (7 files, 21 tests)
  all passed. No emulator inline snapshot churned, confirming the v4 type-token
  change does not reach the four unmasked `normalizeData` calls. End-to-end
  tests N/A per the checklist; the manifest/README changes are covered by task
  2.1's packed-tarball consumer check.

## 1.3 — Upgraded `firebase-admin` to `^14.2.0` (peer + dev); no source change needed

- Key changes:
  - `packages/firebase-kit-admin/package.json` — `firebase-admin` `^13.10.0` →
    `^14.2.0` in **both** `peerDependencies` and `devDependencies`. Range
    **replaced**, not widened (no `||` union).
  - `packages/firebase-kit-admin/README.md:68` — the required-peer bullet now
    quotes `^14.2.0`; the "Used by the root entry point, `./auth`,
    `./firestore`, `./tasks` and `./testing`" sentence left intact.
  - `yarn.lock` — regenerated by `yarn install`. Resolutions moved to
    `firebase-admin` 14.2.0 and, transitively,
    `@google-cloud/firestore` 7.11.6 → 8.7.1; `jwks-rsa` 4.1.0,
    `lru-cache` 11.5.2, `lru-memoizer` 3.0.0 in, and `google-gax`, `jose` 4.x,
    `farmhash-modern`, `@types/long` out.
  - **No source or test file changed.** Zero type errors from the upgrade.
- Precondition: confirmed `firestore-snapshot-utils` appears nowhere in
  `packages/firebase-kit-admin/package.json` (task 1.2 complete), so nothing
  capped `firebase-admin` at 13 any more.
- Deviations from plan: none in substance, but the plan's central prediction did
  not hold. `src/firestore/internal/TransactionWriter.ts` was called out as the
  "most likely breakage site" because its structural interfaces are built on
  `WithFieldValue` and `UpdateData`, the two types `@google-cloud/firestore` 8
  changed. It compiles unchanged — the repo's usage is generic pass-through
  (`WithFieldValue<AppModelType>` / `UpdateData<DbModelType>` forwarded straight
  to `Transaction.set`/`.update`), so the v8 semantic changes (methods now
  ignored on model types; `UpdateData` index-signature support) apply
  identically on both sides of the call and never produce an assignability gap.
  Nothing in `src/firestore/internal/`, `src/mocks/`, `src/tasks/` or
  `src/errors/` needed a change either.
- Every `firebase-admin`-owned breaking change verified inert by grep, not
  assumed: zero bare `from 'firebase-admin'` imports; no `PrefixedFirebaseError`
  or `hasCode` reference anywhere; no `InstanceId`; none of the four dropped
  legacy FCM types; no `CLOUD_TASKS_EMULATOR_HOST`; every manifest already
  declares `engines.node: ">=24"`. The only `FirebaseError` in the repo is
  imported from `firebase/app` (the *client* SDK) in
  `packages/firebase-kit-client/src/testing/createFirebaseFunctionsClientMock.ts`,
  so the error-handling revamp does not reach this package.
- Constraints held: no `any`, `@ts-expect-error`, or new cast was added (no
  source file was touched at all); `typescript` still `^6.0.3` and
  `@types/node` still `^24.13.3` in every manifest.
- Verification: `yarn install` (no peer warning naming `firebase-admin`; the
  four remaining `✘` rows from `yarn explain peer-requirements` are the
  pre-existing `@firebase/*-compat` → `@firebase/app-types` ones in the client
  package). `yarn build` passed from a **clean** tree (`dist/` and every
  `tsconfig.tsbuildinfo` deleted first, so the green build is not a stale
  incremental result). `yarn format` (no changes), `yarn lint`, `yarn test:unit`
  (54 + 29 files, 227 + 149 tests) and `yarn test:emulator` (7 files, 21 tests,
  mandatory for this task) all passed. No auth-emulator test reacted to #3142's
  project-config assertion change. End-to-end tests N/A per the config's
  *consumer-facing wiring* Test exception; its substitute packed-tarball
  consumer check is task 2.1.

## 1.4 — Moved the `getsetdel` range to `^3.0.0` (peer + dev); no code change needed

- Key changes:
  - `packages/firebase-kit-client/package.json` — `getsetdel` `^2.0.0` →
    `^3.0.0` in **both** `peerDependencies` and `devDependencies`. Range
    **replaced**, not widened (no `||` union).
  - `packages/firebase-kit-client/README.md` — install snippet now reads
    `npm install firebase 'getsetdel@^3.0.0'`; the required-peer bullet quotes
    `^3.0.0` (its "IndexedDB store the cached Firestore reads persist through"
    sentence kept); the obsolete "**`getsetdel` must be major 2.**" paragraph and
    its trailing blank line deleted, not rewritten.
  - `yarn.lock` — regenerated by `yarn install` (no `--immutable`). Resolutions:
    `getsetdel` 2.0.0 → 3.0.0 and its dependency range `idb-keyval`
    `^6.2.5` → `^6.3.0`; `idb-keyval` was already resolved at 6.3.0, so no
    transitive version actually moved.
  - **No source or test file changed.** Zero type errors from the upgrade.
- Deviations from plan: none. The plan's prediction held exactly — the `getMany`
  signature change (`T[]` → `(T | undefined)[]`) reaches only the re-export in
  `src/__mocks__/getsetdel/index.ts`, whose result is never consumed, so no call
  site needed to start handling `undefined`. `src/testing/createGetSetDelMock.ts`
  still compiles with its `queryInventory` destructure, since v3 continues to
  export the value.
- Scope held per the gotchas: `src/testing/createGetSetDelMock.ts` and its test,
  `src/__mocks__/getsetdel/index.ts`, `src/__test__/setup/vi.setup.ts`,
  `vitest.config.ts` and the `fake-indexeddb` dev dependency are all untouched;
  nothing from `getsetdel/testing` was adopted (task 1.5) and the `./testing`
  entry point keeps its current name (rename is task 1.5).
- Verification: `yarn install` reported `+ getsetdel@npm:3.0.0` / `-
  getsetdel@npm:2.0.0` with no new peer warning — the four `✘` rows from
  `yarn explain peer-requirements` are the same pre-existing
  `@firebase/*-compat` → `@firebase/app-types` ones. `yarn format` (no changes),
  `yarn lint`, `yarn test:unit` (54 + 29 files, 227 + 149 tests) and
  `yarn test:emulator` (7 files, 21 tests, confirming the admin package is
  unaffected) all passed. `yarn build` passed from a **clean** tree (every
  `dist/` and `tsconfig.tsbuildinfo` deleted first), so the green build is not a
  stale incremental result. End-to-end tests N/A per the config's
  *consumer-facing wiring* Test exception; its substitute packed-tarball
  consumer check is task 2.1.
