# Friction

requirements: goals bootstrap phase 1 ("push to main") is unexecutable under the repo's own `.mise/` guard and the CLAUDE.md never-merge rule — the contradiction survived the goals gate
requirements: goals fixed placeholder version at `0.0.1` while also requiring the first real release be `1.0.0`; conventional-changelog cannot bridge those, so an approved goal was mechanically impossible
requirements: goals recorded scdate as the template for dependabot auto-merge and for provenance without checking what scdate actually does — scdate auto-merges npm majors and has no provenance at all
critic requirements: round 2 found a false claim I had just added to goals (phase-1 push "runs the emulator tests") — writing an unverified upside into an artifact cost a second gate trip
critic requirements: round 3 disproved a fact the whole verification method rested on — npm pack does NOT rewrite the workspace: protocol, only Yarn's packer does; the mise-config test exception had prescribed `npm pack` and would have produced uninstallable tarballs every time
critic requirements: stalled at 3 blocking after 4 rounds (8 -> 4 -> 3 -> 3); each round surfaced new deeper defects rather than re-reporting unfixed ones, so the count plateaued without the document being wrong in the same way twice
critic requirements: rounds 5-7 traded blocking counts 3 -> 2 -> 1; the pattern across all seven rounds was that each fix was correct but revealed a deeper layer (mechanism -> phase scoping -> tool semantics), which a single-round gate would have shipped
critic requirements: round 8 caught that a rule I wrote in round 7 ("no optional peer behind a runtime import") was over-broad and would have forced vitest to be a required peer of both packages; the fix needed a production-vs-test-support entry point distinction that no earlier round had articulated
goals: two dependency-metadata decisions were taken with the maintainer during the requirements stage and left unrecorded in goals.md for a round, so the two approved documents contradicted each other on their face
plan critic: round 1 disproved the versioning mechanism recorded in approved goals decision 8 — with skip-commit true the changelog action reads git TAGS, not package.json, so the 0.0.1 placeholder was never consulted and the first release would have computed 0.1.0; three earlier requirements rounds verified bumpVersion.js without ever checking which strategy feeds it
plan critic: round 1 also found the prescribed phase-1 root tsconfig cannot compile at all (TS18002 on an empty files list with empty references), so task 1.1 could not have ended green
critic plan: 2 rounds, blocking 2 -> 0; round 2's most useful catch was that task 2.1 never restored the phase-1 `build` stub, which would have made every later "yarn build passes" check succeed vacuously with nothing type-checked
task 01_02: review found 2 defects — the 1.0.0 guard read package.json instead of the tag-derived old_version (wrong after a partial release), and the `**/.mise/` pathspec silently matches nothing under default git pathspec magic
task 01_02: the defect was seeded by the plan's own wording — step 9 and its checklist said the guard reads "the current version", which reads as package.json; the tag-derived old_version is what the computation actually uses. Plan prose that names a value imprecisely propagates straight into code.
task 01_03: review found MAINTAINERS.md recommending npm classic tokens, which npm disabled 2025-11-05 and revoked 2025-12-09 — stale-world knowledge in a doc the maintainer executes cold at a hard stop
task 01_04: dependabot activated on repo creation and merged a firebase-tools bump within two minutes, moving main and invalidating the exact version string task 1.3 recorded for task 2.4 to reuse — plan-time constants about mutable dependency state go stale between tasks
task 02_01: Yarn 4's `run` global-script fallback (any script name containing a colon resolves to the single workspace defining it, from any workspace) caused infinite recursion with `test:unit` and triple-execution with `ci:test-emulator`; the plan's test-orchestration design assumed colon names were free. Per-package scripts must be colon-free.
